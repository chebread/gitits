const loading = () => {
  return /* html */ `
    <div class="flex items-center justify-center" style="height: calc(100vh - 164px);">
      
    </div>
  `; // style에 정의된 height는 .app의 x, y center와 동일하다
};

export default loading;
